version: '3.8'

services:
  jeecg-vue3:
    build: .
    ports:
      - "${PORT:-3000}:3000"
    environment:
      - NODE_ENV=production
    volumes:
      - ./public/config.json:/app/dist/config.json
    restart: unless-stopped 