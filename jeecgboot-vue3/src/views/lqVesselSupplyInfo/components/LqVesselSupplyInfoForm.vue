<template>
  <a-spin :spinning="confirmLoading">
    <JFormContainer :disabled="disabled">
      <template #detail>
        <a-form ref="formRef" class="antd-modal-form" :labelCol="labelCol" :wrapperCol="wrapperCol" name="LqVesselSupplyInfoForm">
          <a-row>
						<a-col :span="24">
							<a-form-item label="单位" v-bind="validateInfos.unit" id="LqVesselSupplyInfoForm-unit" name="unit">
								<a-input v-model:value="formData.unit" placeholder="请输入单位"  allow-clear ></a-input>
							</a-form-item>
						</a-col>
						<a-col :span="24">
							<a-form-item label="填报时间" v-bind="validateInfos.reportTime" id="LqVesselSupplyInfoForm-reportTime" name="reportTime">
								<a-date-picker placeholder="请选择填报时间"  v-model:value="formData.reportTime" value-format="YYYY-MM-DD"  style="width: 100%"  allow-clear />
							</a-form-item>
						</a-col>
						<a-col :span="24">
							<a-form-item label="舷号" v-bind="validateInfos.shipNumber" id="LqVesselSupplyInfoForm-shipNumber" name="shipNumber">
								<a-input v-model:value="formData.shipNumber" placeholder="请输入舷号"  allow-clear ></a-input>
							</a-form-item>
						</a-col>
						<a-col :span="24">
							<a-form-item label="剩余燃油（吨）" v-bind="validateInfos.remainingFuel" id="LqVesselSupplyInfoForm-remainingFuel" name="remainingFuel">
								<a-input-number v-model:value="formData.remainingFuel" placeholder="请输入剩余燃油（吨）" style="width: 100%" />
							</a-form-item>
						</a-col>
						<a-col :span="24">
							<a-form-item label="剩余滑油（吨）" v-bind="validateInfos.remainingLubricatingOil" id="LqVesselSupplyInfoForm-remainingLubricatingOil" name="remainingLubricatingOil">
								<a-input-number v-model:value="formData.remainingLubricatingOil" placeholder="请输入剩余滑油（吨）" style="width: 100%" />
							</a-form-item>
						</a-col>
						<a-col :span="24">
							<a-form-item label="剩余淡水（吨）" v-bind="validateInfos.remainingFreshWater" id="LqVesselSupplyInfoForm-remainingFreshWater" name="remainingFreshWater">
								<a-input-number v-model:value="formData.remainingFreshWater" placeholder="请输入剩余淡水（吨）" style="width: 100%" />
							</a-form-item>
						</a-col>
						<a-col :span="24">
							<a-form-item label="剩余主食（天）" v-bind="validateInfos.remainingStapleFoodDays" id="LqVesselSupplyInfoForm-remainingStapleFoodDays" name="remainingStapleFoodDays">
								<a-input-number v-model:value="formData.remainingStapleFoodDays" placeholder="请输入剩余主食（天）" style="width: 100%" />
							</a-form-item>
						</a-col>
						<a-col :span="24">
							<a-form-item label="剩余副食（天）" v-bind="validateInfos.remainingNonStapleFoodDays" id="LqVesselSupplyInfoForm-remainingNonStapleFoodDays" name="remainingNonStapleFoodDays">
								<a-input-number v-model:value="formData.remainingNonStapleFoodDays" placeholder="请输入剩余副食（天）" style="width: 100%" />
							</a-form-item>
						</a-col>
						<a-col :span="24">
							<a-form-item label="影响任务安全故障" v-bind="validateInfos.safetyFault" id="LqVesselSupplyInfoForm-safetyFault" name="safetyFault">
								<a-textarea v-model:value="formData.safetyFault" :rows="4" placeholder="请输入影响任务安全故障" />
							</a-form-item>
						</a-col>
						<a-col :span="24">
							<a-form-item label="燃油总容量（吨）" v-bind="validateInfos.fuelTotalCapacity" id="LqVesselSupplyInfoForm-fuelTotalCapacity" name="fuelTotalCapacity">
								<a-input-number v-model:value="formData.fuelTotalCapacity" placeholder="请输入燃油总容量（吨）" style="width: 100%" />
							</a-form-item>
						</a-col>
						<a-col :span="24">
							<a-form-item label="滑油总容量（吨）" v-bind="validateInfos.lubricatingOilTotalCapacity" id="LqVesselSupplyInfoForm-lubricatingOilTotalCapacity" name="lubricatingOilTotalCapacity">
								<a-input-number v-model:value="formData.lubricatingOilTotalCapacity" placeholder="请输入滑油总容量（吨）" style="width: 100%" />
							</a-form-item>
						</a-col>
						<a-col :span="24">
							<a-form-item label="淡水总容量（吨）" v-bind="validateInfos.freshWaterTotalCapacity" id="LqVesselSupplyInfoForm-freshWaterTotalCapacity" name="freshWaterTotalCapacity">
								<a-input-number v-model:value="formData.freshWaterTotalCapacity" placeholder="请输入淡水总容量（吨）" style="width: 100%" />
							</a-form-item>
						</a-col>
						<a-col :span="24">
							<a-form-item label="主食总量（天）" v-bind="validateInfos.stapleFoodTotalDays" id="LqVesselSupplyInfoForm-stapleFoodTotalDays" name="stapleFoodTotalDays">
								<a-input-number v-model:value="formData.stapleFoodTotalDays" placeholder="请输入主食总量（天）" style="width: 100%" />
							</a-form-item>
						</a-col>
						<a-col :span="24">
							<a-form-item label="副食总量（天）" v-bind="validateInfos.nonStapleFoodTotalDays" id="LqVesselSupplyInfoForm-nonStapleFoodTotalDays" name="nonStapleFoodTotalDays">
								<a-input-number v-model:value="formData.nonStapleFoodTotalDays" placeholder="请输入副食总量（天）" style="width: 100%" />
							</a-form-item>
						</a-col>
						<a-col :span="24">
							<a-form-item label="填报人" v-bind="validateInfos.reporter" id="LqVesselSupplyInfoForm-reporter" name="reporter">
								<a-input v-model:value="formData.reporter" placeholder="请输入填报人"  allow-clear ></a-input>
							</a-form-item>
						</a-col>
          </a-row>
        </a-form>
      </template>
    </JFormContainer>
  </a-spin>
</template>

<script lang="ts" setup>
  import { ref, reactive, defineExpose, nextTick, defineProps, computed, onMounted } from 'vue';
  import { defHttp } from '/@/utils/http/axios';
  import { useMessage } from '/@/hooks/web/useMessage';
  import { getValueType } from '/@/utils';
  import { saveOrUpdate } from '../LqVesselSupplyInfo.api';
  import { Form } from 'ant-design-vue';
  import JFormContainer from '/@/components/Form/src/container/JFormContainer.vue';
  const props = defineProps({
    formDisabled: { type: Boolean, default: false },
    formData: { type: Object, default: () => ({})},
    formBpm: { type: Boolean, default: true }
  });
  const formRef = ref();
  const useForm = Form.useForm;
  const emit = defineEmits(['register', 'ok']);
  const formData = reactive<Record<string, any>>({
    id: '',
    unit: '',   
    reportTime: '',   
    shipNumber: '',   
    remainingFuel: undefined,
    remainingLubricatingOil: undefined,
    remainingFreshWater: undefined,
    remainingStapleFoodDays: undefined,
    remainingNonStapleFoodDays: undefined,
    safetyFault: '',   
    fuelTotalCapacity: undefined,
    lubricatingOilTotalCapacity: undefined,
    freshWaterTotalCapacity: undefined,
    stapleFoodTotalDays: undefined,
    nonStapleFoodTotalDays: undefined,
    reporter: '',   
  });
  const { createMessage } = useMessage();
  const labelCol = ref<any>({ xs: { span: 24 }, sm: { span: 5 } });
  const wrapperCol = ref<any>({ xs: { span: 24 }, sm: { span: 16 } });
  const confirmLoading = ref<boolean>(false);
  //表单验证
  const validatorRules = reactive({
  });
  const { resetFields, validate, validateInfos } = useForm(formData, validatorRules, { immediate: false });

  // 表单禁用
  const disabled = computed(()=>{
    if(props.formBpm === true){
      if(props.formData.disabled === false){
        return false;
      }else{
        return true;
      }
    }
    return props.formDisabled;
  });

  
  /**
   * 新增
   */
  function add() {
    edit({});
  }

  /**
   * 编辑
   */
  function edit(record) {
    nextTick(() => {
      resetFields();
      const tmpData = {};
      Object.keys(formData).forEach((key) => {
        if(record.hasOwnProperty(key)){
          tmpData[key] = record[key]
        }
      })
      //赋值
      Object.assign(formData, tmpData);
    });
  }

  /**
   * 提交数据
   */
  async function submitForm() {
    try {
      // 触发表单验证
      await validate();
    } catch ({ errorFields }) {
      if (errorFields) {
        const firstField = errorFields[0];
        if (firstField) {
          formRef.value.scrollToField(firstField.name, { behavior: 'smooth', block: 'center' });
        }
      }
      return Promise.reject(errorFields);
    }
    confirmLoading.value = true;
    const isUpdate = ref<boolean>(false);
    //时间格式化
    let model = formData;
    if (model.id) {
      isUpdate.value = true;
    }
    //循环数据
    for (let data in model) {
      //如果该数据是数组并且是字符串类型
      if (model[data] instanceof Array) {
        let valueType = getValueType(formRef.value.getProps, data);
        //如果是字符串类型的需要变成以逗号分割的字符串
        if (valueType === 'string') {
          model[data] = model[data].join(',');
        }
      }
    }
    await saveOrUpdate(model, isUpdate.value)
      .then((res) => {
        if (res.success) {
          createMessage.success(res.message);
          emit('ok');
        } else {
          createMessage.warning(res.message);
        }
      })
      .finally(() => {
        confirmLoading.value = false;
      });
  }


  defineExpose({
    add,
    edit,
    submitForm,
  });
</script>

<style lang="less" scoped>
  .antd-modal-form {
    padding: 14px;
  }
</style>
