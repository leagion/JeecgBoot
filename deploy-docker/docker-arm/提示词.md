我的要求：1.在docker-arm文件夹内实现我的全部要求，不要在其他文件夹生成文件。
2.我要在windows10 X64 上构建了ARM镜像，然后就将将镜像和数据卷传输到内网麒麟ARM64系统部署。
3.生成一个详细可操作方案文件md、配置文件yml 、env环境配置文件，然后没个镜像一个文件夹（包括镜像、数据卷、构建文件、运行脚本等）一个一个镜像构建，但是都加入统一networks
4.参考 **@docker-compose-lq.yml**@start-docker-compose-lq.bat 拆分成一个个镜像单独构建，但是同一个网络，镜像之间可以互相调用访问。

**优化后：**

请根据以下要求制定一套完整的Docker ARM镜像构建与跨平台部署方案：

1. 所有操作必须严格限定在 `docker-arm`文件夹内执行，禁止在项目其他目录创建或修改任何文件。
2. 目标是在Windows 10 x64环境下为ARM架构构建Docker镜像，并将这些镜像及对应的数据卷完整迁移至内网的麒麟ARM64操作系统进行部署。
3. 输出成果应包含：

   - 一份详细的实施方案文档（Markdown格式），涵盖构建流程、迁移步骤和部署指南
   - 统一的Docker Compose配置文件（YML格式）用于定义服务间的网络通信
   - 全局环境变量配置文件（.env）
   - 每个独立服务需建立专属子目录，其结构如下：
     ```
     docker-arm/
     ├── {service-name}/
     │   ├── Dockerfile （如需要自定义构建）
     │   ├── docker-compose-{service}.yml （单个服务的compose配置）
     │   ├── volumes/ （该服务专用的数据持久化目录）
     │   ├── build/ （构建所需资源文件）
     │   └── run.sh 或 run.bat （服务启动脚本）
     ```
4. 参考现有文件 `docker-compose-lq.yml`的服务定义以及 `start-docker-compose-lq.bat`的执行逻辑，对每个服务进行拆分以便单独构建和运行，但所有服务必须加入同一个名为 `aiccg-networks`的自定义桥接网络，确保容器间可通过服务名相互访问。

特别注意：保持原有服务依赖关系不变，仅调整组织形式以适应ARM架构构建和跨平台部署需求。
