mind-elixir 集成到jeecgboot 具体功能需求：

1数据库用postgresql（特别注意给sql的相关要求，PostgreSQL 不支持在字段定义时直接用 `COMMENT`，而是需要通过 `COMMENT ON COLUMN` 语句单独添加注释；），创建表 lq_mindmap ，Mind Elixir 将所有内容存储在数据库或者计算机本地，主表只存储最新版本，减少查询最新数据的开销；版本表添加联合索引（`mindmap_id + version`） 需确保版本号自增，避免重复，加速版本查询；`content` 压缩采用 PostgreSQL 内置 `pgcrypto` 函数（如 `pgp_sym_encrypt`/`pgp_sym_decrypt`）

2.复用JeecgBoot 内置的 AI 模块，在思维导图中添加 AI 生成节点功能，调用 AI 时显示加载状态；支持 “取消生成” 功能；缓存常见 AI 生成结果。缓存常见 AI 生成结果时，关联思维导图主题，提升命中率；

* 加载状态采用 “骨架屏 + 进度条” 结合

3标签分组管理，用标签轻松分类和查找思维导图，让知识管理井然有序。标签筛选需优化查询（如联合索引 `(user_id + tag_id)`）。

4.具有大纲模式

5.jeecgboot登录用户保存自己的思维导图,自动打开用户最新思维导图，**权限细化** ：除 “用户只能查看自己的思维导图” 外，增加 “共享功能”（如指定用户 / 角色访问），适配团队协作场景。

6.菜单栏增加思维导图，

7.具有Data Import and Export功能，思维导图导出为多种格式，包括 JSON、PNG、JPEG、WEBP、HTML 和 Markdown，方便在不同场景中分享或使用您的思维导图。

**8自动保存** ：定时自动创建版本（如每 10 分钟），避免用户忘记手动保存。10 分钟间隔需可配置（不同用户需求不同）；网络波动时可能导致重复保存，需加防抖处理（如 30 秒内仅保存一次）。自动保存触发时，前端显示 “正在自动保存...” 弱提示，避免干扰用户；

9.具有历史版本记录，采用增量存储（仅记录变更部分，而非完整数据）；限制最大版本数量（如仅保留最近 50 个版本）。实现两个版本内容的差异对比（可通过 JSON 差异算法，如 `jsondiffpatch` 库）,版本差异对比使用可视化组件（如 `jsondiffpatch-react`），高亮显示增删改节点。。允许删除老旧版本（保留关键版本），释放存储空间。**版本回滚** ：回滚时不直接修改历史版本，而是将目标版本的内容复制到主表，并生成一个新的版本记录（标注 “回滚至 xxx 版本”），确保历史可追溯。版本表增加 `is_key_version` 字段（标记关键版本），删除老旧版本时保留关键版本，减少人工干预。

10具有以下插件功能@mind-elixir/node-menu,@mind-elixir/node-menu-neo,@mind-elixir/export-xmind,@mind-elixir/export-html，

实现以上功能，按以下步骤：

1. 先完成核心功能（表设计、基础 CRUD、思维导图渲染）；
2. 逐步集成标签、历史版本、导入导出等增强功能；
3. 最后对接 AI 接口和 MCP 集成，利用 JeecgBoot 现有生态简化开发。

分步骤给出清晰的前后端详细代码，优先使用Jeecgboot online表单开发，低代码开发功能，下面给出第一个步骤代码。
